import { Node } from 'three/webgpu';

/**
 * Applies an inverted luminance mask to the target node
 * Uses the inverted brightness (luminance) of the mask to determine opacity
 *
 * @param target - The node to mask
 * @param mask - The node to use as a mask
 * @returns Masked node with the inverted luminance-based masking applied
 */
export default function (target: Node, mask: Node): import('three/webgpu').JoinNode;
//# sourceMappingURL=luminanceInverted.d.ts.map