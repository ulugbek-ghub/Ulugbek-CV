import { Node } from 'three/webgpu';

/**
 * Applies a luminance mask to the target node
 * Uses the brightness (luminance) of the mask to determine opacity
 *
 * @param target - The node to mask
 * @param mask - The node to use as a mask
 * @returns Masked node with luminance-based masking applied
 */
export default function (target: Node, mask: Node): import('three/webgpu').JoinNode;
//# sourceMappingURL=luminance.d.ts.map