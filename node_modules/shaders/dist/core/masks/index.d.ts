import { Node } from 'three/webgpu';
import { MaskType } from '../types';

/**
 * Applies a mask to a target node using the specified mask type
 *
 * @param target - The node to mask
 * @param mask - The node to use as a mask
 * @param maskType - The type of masking to apply
 * @returns Masked node
 */
export declare function applyMask(target: Node, mask: Node, maskType?: MaskType): Node;
//# sourceMappingURL=index.d.ts.map