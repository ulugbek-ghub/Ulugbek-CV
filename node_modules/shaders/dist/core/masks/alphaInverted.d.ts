import { Node } from 'three/webgpu';

/**
 * Applies an inverted alpha mask to the target node
 *
 * @param target - The node to mask
 * @param mask - The node to use as a mask
 * @returns Masked node with the inverted alpha channel from mask applied
 */
export default function (target: Node, mask: Node): import('three/webgpu').JoinNode;
//# sourceMappingURL=alphaInverted.d.ts.map