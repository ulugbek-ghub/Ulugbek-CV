import { ComponentDefinition } from '../../types';
import { transformPosition } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Overall animation speed
     *
     * Accepts a number between 0 and 1.
     * @default 0.5
     */
    speed: number;
    /**
     * Wave height amplitude
     *
     * @default 1
     */
    amplitude: number;
    /**
     * Wave frequency
     *
     * @default 1
     */
    frequency: number;
    /**
     * Number of wave lines
     *
     * Accepts a number between 4 and 32.
     * @default 12
     */
    lineCount: number;
    /**
     * Width of wave lines
     *
     * @default 0.1
     */
    lineWidth: number;
    /**
     * Color of the waves
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#f1c907"
     */
    waveColor: string;
    /**
     * Pin waves at edges (fade effect)
     *
     * Accepts a boolean value (`true` or `false`).
     * @default true
     */
    pinEdges: boolean;
    /**
     * Starting point of the waves
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":0,"y":0.5}
     */
    start: Parameters<typeof transformPosition>[0];
    /**
     * Ending point of the waves
     *
     * Accepts `{ x, y }` objects with values from 0 to 1, or CSS positions like `top center`.
     * @default {"x":1,"y":0.5}
     */
    end: Parameters<typeof transformPosition>[0];
}
export declare const componentDefinition: ComponentDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map