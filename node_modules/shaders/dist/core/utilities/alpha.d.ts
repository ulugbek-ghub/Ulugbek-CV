import { Node } from 'three/webgpu';

/**
 * Converts a premultiplied-alpha color to straight alpha.
 *
 * RTT boundaries in the renderer produce premultiplied data because
 * blending over a transparent base multiplies RGB by alpha. Effects
 * that sample from RTT textures (blur, distortion, color processing)
 * must convert back to straight alpha before returning, since the
 * blend mode composition pipeline expects straight alpha.
 *
 * For fully opaque content (alpha = 1), this is a no-op.
 */
export declare const unpremultiplyAlpha: (color: Node) => Node;
//# sourceMappingURL=alpha.d.ts.map