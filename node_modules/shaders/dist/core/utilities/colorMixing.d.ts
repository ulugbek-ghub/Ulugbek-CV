import { Node } from 'three/webgpu';

/**
 * Mix two colors in the specified color space
 *
 * @param colorA - First color (vec4 with RGBA in P3-linear)
 * @param colorB - Second color (vec4 with RGBA in P3-linear)
 * @param t - Interpolation factor (0 = colorA, 1 = colorB)
 * @param colorSpaceMode - Color space mode as a plain JS number (resolved at compile time):
 *   0 = linear RGB, 1 = OKLCh, 2 = OKLAB, 3 = HSL, 4 = HSV, 5 = LCH
 * @returns Mixed color as vec4 in P3-linear
 *
 * PERFORMANCE NOTE: colorSpaceMode must be a plain JS number, not a TSL uniform Node.
 * Passing a Node caused all 6 color space paths to be compiled unconditionally,
 * adding ~435 TSL nodes per call. Using a JS switch here means only one path
 * is compiled into the WGSL shader, dramatically reducing node count.
 */
export declare const mixColors: (colorA: Node, colorB: Node, t: Node, colorSpaceMode: number) => Node;
//# sourceMappingURL=colorMixing.d.ts.map