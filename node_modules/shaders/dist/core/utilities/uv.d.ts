/**
 * Applies aspect ratio correction to a given 2D point.
 * Multiplies the x-coordinate by the viewport aspect ratio.
 *
 * @param point The input vec2 point (x, y) to correct.
 * @returns A new vec2 point with aspect ratio correction applied.
 */
export declare const applyAspectRatioCorrection: (point?: import('three/webgpu').ConstNode<import('three').Vector2>) => import('three/webgpu').JoinNode;
//# sourceMappingURL=uv.d.ts.map