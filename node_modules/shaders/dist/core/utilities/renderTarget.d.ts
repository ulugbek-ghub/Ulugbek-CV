import { Node } from 'three/webgpu';
import { ResizeCallback, RenderCallback } from '../types';

/**
 * A texture node that can be sampled with custom UVs
 */
export interface SamplableTextureNode extends Node {
    sample: (uv: Node) => Node;
}
/**
 * Creates a resizable render target for RTT effects
 * @param renderer - The Three.js renderer instance
 * @param childNode - The shader node to render to the texture
 * @param width - Initial width
 * @param height - Initial height
 * @param onCleanup - Cleanup callback to dispose resources
 * @param onResize - Resize callback to register for viewport changes
 * @param onBeforeRender - Before render callback to render child to texture
 * @returns A texture node that can be sampled in shaders
 */
export declare function createResizableRenderTarget(renderer: any, childNode: Node, width: number, height: number, onCleanup: (callback: () => void) => void, onResize: (callback: ResizeCallback) => void, onBeforeRender: (callback: RenderCallback) => void): SamplableTextureNode;
//# sourceMappingURL=renderTarget.d.ts.map