import { Vector2, Vector4 } from 'three';

export declare const setColorSpaceMode: (mode: "p3-linear" | "srgb") => void;
/**
 * Convert a true/false boolean into an integer 1/-1 representation
 * @param value
 */
export declare const transformBoolean: (value: boolean) => 1 | -1;
/**
 * Convert a hex color or HTML color code into both TSL node and Three.js Vector4
 * Uses the globally configured color space mode (p3-linear or srgb)
 * @param value
 * @returns Object with node (TSL vec4 for shader composition) and data (Vector4 for uniform updates)
 */
export declare const transformColor: (value: string) => {
    node: any;
    data: Vector4;
};
/**
 * Update-optimized color transform that reuses a single Vector4 instance.
 * ONLY use for uniform updates - the returned vector is reused and will be overwritten.
 * Uses the globally configured color space mode (p3-linear or srgb)
 * @param value - Color string (hex, rgb, etc.)
 * @returns Reusable Vector4 with color data (do NOT store reference long-term)
 */
export declare const transformColorUpdate: (value: string) => Vector4;
/**
 * Converts a position value into both TSL node and raw data.
 * Accepts either an object {x: number, y: number} or a CSS-style string
 * like "top left", "center", "bottom right", etc.
 * Normalizes coordinates to a 0-1 range and inverts the y-axis
 * (so y=0 is top, y=1 is bottom, aligning with typical screen coordinates).
 *
 * @param value - The position value, either {x, y} or a string.
 * @returns Object with node (for initial uniform creation) and data (for updates)
 */
export declare const transformPosition: (value: {
    x: number | string;
    y: number | string;
} | string) => {
    node: any;
    data: Vector2;
};
/**
 * Update-optimized position transform that reuses a single Vector2 instance.
 * ONLY use for uniform updates - the returned vector is reused and will be overwritten.
 * @param value - Position value ({x, y} object or CSS-style string)
 * @returns Reusable Vector2 with position data (do NOT store reference long-term)
 */
export declare const transformPositionUpdate: (value: {
    x: number | string;
    y: number | string;
} | string) => Vector2;
/**
 * Transform angle values into normalized degrees (0-360).
 * Supports numerical angles with automatic wrapping and CSS-style gradient directions.
 *
 * @param value - Angle value (number or CSS gradient direction string)
 * @returns Normalized angle in degrees (0-360)
 */
export declare const transformAngle: (value: number | string) => number;
/**
 * Transform edge handling mode strings into numeric values for shader use.
 * Controls how content behaves when distorted UVs fall outside the 0-1 range.
 *
 * @param value - Edge mode string ('stretch', 'transparent', 'mirror', or 'wrap')
 * @returns Numeric mode value (0 = stretch, 1 = transparent, 2 = mirror, 3 = wrap)
 */
export declare const transformEdges: (value: string) => number;
/**
 * Color space options for UI select dropdowns
 * Centralized configuration for color interpolation modes
 */
export declare const colorSpaceOptions: {
    label: string;
    value: string;
}[];
/**
 * Transform color space interpolation mode strings into numeric values for shader use.
 * Controls how colors are mixed/interpolated in gradient effects.
 *
 * @param value - Color space string ('linear', 'oklch', 'oklab', 'hsl', 'hsv', or 'lch')
 * @returns Numeric mode value (0 = linear RGB, 1 = OKLCh, 2 = OKLAB, 3 = HSL, 4 = HSV, 5 = LCH)
 */
export declare const transformColorSpace: (value: string) => number;
//# sourceMappingURL=transformations.d.ts.map