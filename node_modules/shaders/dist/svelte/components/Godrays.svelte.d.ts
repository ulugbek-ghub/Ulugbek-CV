import { type BlendMode, type PropDriver, type TransformConfig } from '../../core';
import { type ComponentProps } from '../../core/shaders/Godrays';
/**
 * Define component props including blend mode, opacity, visibility, masking, and transformation
 */
interface ExtendedComponentProps extends Partial<ComponentProps> {
    center?: ComponentProps['center'] | PropDriver;
    density?: ComponentProps['density'] | PropDriver;
    intensity?: ComponentProps['intensity'] | PropDriver;
    spotty?: ComponentProps['spotty'] | PropDriver;
    blendMode?: BlendMode;
    opacity?: number;
    visible?: boolean;
    id?: string;
    maskSource?: string;
    maskType?: string;
    renderOrder?: number;
    transform?: Partial<TransformConfig>;
    children?: import('svelte').Snippet;
}
declare const Godrays: import("svelte").Component<ExtendedComponentProps, {}, "">;
type Godrays = ReturnType<typeof Godrays>;
export default Godrays;
