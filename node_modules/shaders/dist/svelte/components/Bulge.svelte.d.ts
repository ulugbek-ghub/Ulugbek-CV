import { type BlendMode, type PropDriver, type TransformConfig } from '../../core';
import { type ComponentProps } from '../../core/shaders/Bulge';
/**
 * Define component props including blend mode, opacity, visibility, masking, and transformation
 */
interface ExtendedComponentProps extends Partial<ComponentProps> {
    center?: ComponentProps['center'] | PropDriver;
    strength?: ComponentProps['strength'] | PropDriver;
    radius?: ComponentProps['radius'] | PropDriver;
    falloff?: ComponentProps['falloff'] | PropDriver;
    blendMode?: BlendMode;
    opacity?: number;
    visible?: boolean;
    id?: string;
    maskSource?: string;
    maskType?: string;
    renderOrder?: number;
    transform?: Partial<TransformConfig>;
    children?: import('svelte').Snippet;
}
declare const Bulge: import("svelte").Component<ExtendedComponentProps, {}, "">;
type Bulge = ReturnType<typeof Bulge>;
export default Bulge;
